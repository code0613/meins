# 기여 가이드

## 브랜치 전략

```
main            배포 가능한 상태만 둡니다. dev에서 검증된 것만 올라옵니다.
 └ dev          기본 브랜치. 모든 작업이 여기로 먼저 모입니다.
    └ feature/12_add_fill_mode
    └ fix/15_undo_history
```

작업은 항상 `dev`에서 갈라져 나와 `dev`로 돌아옵니다.
`dev`에서 테스트가 끝나면 `main`으로 머지합니다.

**브랜치 이름**

```
feature/<이슈번호>_<한_일>
fix/<이슈번호>_<한_일>
```

예) `feature/12_add_fill_mode`, `fix/15_undo_history`

## 커밋 메시지

```
<type>: <설명>
```

괄호로 감싸는 scope는 쓰지 않습니다.

| type | 쓰는 곳 |
|---|---|
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `docs` | 문서만 |
| `style` | 포맷 등 동작 변화 없음 |
| `refactor` | 동작 그대로, 구조 변경 |
| `perf` | 성능 개선 |
| `test` | 테스트 |
| `build` | 빌드 설정, 의존성 |
| `ci` | CI 설정 |
| `chore` | 그 외 |

```
feat: 불투명 채우기 모드 추가
fix: 되돌리기가 두 단계씩 지워지는 문제 수정
```

제목은 50자 이내, 끝에 마침표를 붙이지 않습니다.
본문이 필요하면 한 줄 띄고 **왜 그렇게 했는지**를 적습니다. 무엇을 했는지는 diff가 말해줍니다.

## 이슈

코드를 고치는 작업은 이슈를 먼저 만듭니다. 브랜치 이름과 PR에 참조 번호가 생기고,
나중에 "왜 이렇게 만들었는지"를 추적할 수 있습니다.

이슈는 **현상 → 문제 → 원인 → 해결 방향** 순으로 적습니다.
해결 방향에는 **검토했던 다른 방법**과 **최종안을 고른 이유·트레이드오프**를 함께 남깁니다.
템플릿이 그 순서대로 준비돼 있습니다.

## PR

- 제목도 커밋 컨벤션을 따릅니다 (`feat: ...`). squash merge하면 제목이 커밋 메시지가 됩니다.
- 본문은 PR 템플릿을 채웁니다.
- 대상 브랜치는 `dev`입니다.
- 본문에 `Closes #12`를 넣으면 `dev`로 머지될 때 이슈가 자동으로 닫힙니다.
- 머지는 **Squash and merge**를 씁니다.

## 배포와 릴리스

`main`이 프로덕션 배포 브랜치입니다. Vercel이 `main`을 프로덕션으로,
`dev`를 프리뷰로 배포합니다.

```
feature/12_...  ─PR→  dev  ─release PR→  main  ─자동 배포→  프로덕션
```

`dev`에서 확인이 끝나면 `dev → main` PR을 올립니다.
이 PR만은 커밋 컨벤션을 따르지 않고 **날짜 기반 이름**을 씁니다.

| 상황 | PR 제목 |
|---|---|
| 일반 배포 | `release_YYMMDD` |
| 긴급 수정 배포 | `hotfix_YYMMDD` |

```
release_260826
hotfix_260826
```

같은 날 두 번 이상 올릴 일이 생기면 뒤에 순번을 붙입니다 (`release_260826_2`).

릴리스 PR 본문에는 이번 배포에 포함되는 변경을 나열합니다.
개별 내용은 이미 각 PR과 이슈에 있으므로 목록과 링크로 충분합니다.

## 라벨

| 라벨 | 쓰는 곳 |
|---|---|
| `bug` | 동작이 기대와 다름 |
| `feature` | 새 기능 |
| `refactor` | 동작 그대로, 구조 개선 |
| `docs` | 문서 |
| `blocked` | 다른 작업이나 결정을 기다리는 중 |

## 마일스톤

버전 단위로 끊습니다 (`v0.1.0`, `v0.2.0`).
기간이 아니라 **범위**를 정하는 장치로 씁니다.

## 트러블슈팅 기록

배포나 빌드처럼 **환경 때문에 막혔던 문제**는 해결한 뒤
[docs/troubleshooting.md](./docs/troubleshooting.md)에 한 항목 추가합니다.

자세한 경위는 이슈에 남기고, 이 문서에는
**증상 / 원인 / 해결 / 다음에 볼 것**만 압축해 적습니다. 발생일과 이슈 링크를 함께 남깁니다.

목적은 기록 자체가 아니라 **같은 문제를 다시 만났을 때 어디부터 볼지**를 남기는 것입니다.

## 로컬 확인

PR 올리기 전에 아래 둘은 반드시 통과해야 합니다.

```bash
yarn lint
yarn build
```

이 프로젝트는 이미지를 기기 밖으로 내보내지 않는 것이 핵심입니다.
운영 빌드에는 `connect-src 'none'` CSP가 들어가므로, 외부 통신이 필요한 코드는 추가하지 마세요.

## AI 에이전트로 작업한다면

작업 규칙은 [AGENTS.md](./AGENTS.md)에 있습니다. 이 문서와 같은 내용이지만
에이전트가 읽기 좋은 형태로 정리돼 있고, 코드 스타일과 주석 정책이 더 들어 있습니다.

Claude Code는 `AGENTS.md`를 직접 읽지 않습니다. 저장소 루트의 `CLAUDE.md`가
한 줄짜리 임포트로 연결해 두었으니 그대로 두세요.

Cursor나 Copilot을 쓴다면 각 도구의 설정 파일에서 `AGENTS.md`를 가리키게 하면 됩니다.
규칙 본문을 복사하지 마세요. 정본은 한 곳이어야 합니다.

